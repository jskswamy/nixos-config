# Darwin-specific shell scripts
{
  user,
  config,
  pkgs,
}: {
  # Add Darwin-specific scripts here when needed
  # Example:
  # ".local/bin/darwin-specific-script" = {
  #   source = ./scripts/darwin-specific-script;
  #   executable = true;
  # };
}
