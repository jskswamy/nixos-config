# NixOS-specific shell scripts
{ user }:

{
  # Add NixOS-specific scripts here when needed
  # Example:
  # ".local/bin/nixos-specific-script" = {
  #   source = ./scripts/nixos-specific-script;
  #   executable = true;
  # };
}